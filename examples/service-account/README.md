# Service Account Example

This directory show-cases how to write an application that uses programmatic access to the dApp UI via Service Accounts.

This example uses Python to demonstrate API access, and is setup using `uv`: https://docs.astral.sh/uv/getting-started/installation/

## Run

`uv run main.py`
